# generated by modules/crypto/build.sh
modules/crypto/*.c
modules/crypto/sdk.cicili

# and by modules/math/build.sh -- the same shape, missed when math was
# added. It showed up when a change to COCOLOG'S SDK regenerated the C
# and dirtied this repository: a tracked generated file makes every
# upstream change look like a change here.
modules/math/*.c
modules/math/sdk.cicili
library/*.so
test/*.log
test/*.out
